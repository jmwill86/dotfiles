set editing-mode vi                                            

$if mode=vi                
    # normal mode                           
    set keymap vi-command                                 
    "g": beginning-of-history
    "G": end-of-history
    # insert mode             
    set keymap vi-insert                          
    "jj": vi-movement-mode # remap escape   
$endif 
